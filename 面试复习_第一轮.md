# AI 应用开发实习生面试 — 第一轮总复习表

> 项目:ai-code-review(LangGraph 多 Agent 代码审查)
> 范围:Q1–Q9 考点 + 亲手修复的线上 bug + 系统限流清单 + 面试话术

---

## 一、9 道题考点速览

| 题 | 子系统 | 核心考点 |
|---|---|---|
| Q1/Q2 | 审查 Agent | 4 个 Agent 是否真并行;LLM API 限流与熔断器 |
| Q3 | LangGraph | 图级并行(逻辑并发)vs 信号量(物理限流)两层;state 合并 |
| Q4 | Agent 记忆 | **不是 RAG**,是 per-file 精确匹配的键值记忆;无 resolved 字段 → 幽灵 finding |
| Q5 | LLM 输出 | JSON 提取/解析可靠性;**失败要响亮**,别静默返回空数组 |
| Q6 | Celery | PENDING 陷阱;无条件重试烧钱;**跨进程信号量失效** |
| Q7 | WebSocket | 假进度;断开阻塞事件循环;并发无上限 |
| Q8 | MCP 安全 | 任意路径读文件;**prompt injection**;黑名单/白名单/脱敏 |
| Q9 | IP 限流 | 内存泄漏;反代后 IP 不准;多进程失效;**换 IP 刷 → 鉴权** |

---

## 二、核心概念(面试必会,一句话版)

| 概念 | 一句话 |
|---|---|
| **进程** | 一个运行的程序,有独立内存。像一家独立的餐厅 |
| **线程** | 进程内部的一条执行线,共享进程内存。像餐厅里的厨师 |
| **并发/并行** | 多个任务同时推进(线程)/ 同一瞬间真正同时跑 |
| **同步/异步** | 傻等 vs 不等。async = 不阻塞,靠 await 切换 |
| **信号量** | 门卫,限"同时几个在飞"。**存在进程内存里 → 每进程一个** |
| **WebSocket** | 实时通信通道(电话线),负责"通知" |
| **Celery** | 任务队列(厨房),负责"干活"。WS 只当前台,Celery 当后厨 |
| **反向代理** | 服务器前的接待员,隐藏后端、转发、统一入口 |
| **X-Forwarded-For** | nginx 贴的"真实 IP"便条。**能伪造,只信自家 nginx 递的** |
| **限流层次** | 网关(nginx)→ 任务(Celery concurrency)→ LLM(信号量)→ 重试/熔断 |

---

## 三、亲手修复的 bug(真实项目经历,面试讲这些)

### 1. PENDING 陷阱(Q6)
- **问题**: 查不存在的 task_id,Celery 也返回 PENDING"排队中",用户永远等
- **修法**: `tasks` 表落库,查询先查库,无则 404;超 7 天返回 EXPIRED
- **话术**: "我发现了假 PENDING,用任务注册表区分'不存在'和'在排队'"

### 2. 无条件重试烧钱(Q6)
- **问题**: 任何异常都重试 2 次,每次重跑整轮审查(4 Agent × N 次 LLM)
- **修法**: `_is_retryable_error` 只对瞬时错误(限流/超时/5xx)重试 + 指数退避;永久错误直接失败
- **话术**: "重试要区分永久/瞬时错误,瞬时才重试,且要退避"

### 3. WebSocket 三宗罪(Q7)
- **问题**: ① 假进度(硬编码 50%)② 断开阻塞事件循环(`shutdown(wait=True)`)③ 每连接一个线程池无上限
- **修法**: WS 复用 Celery——`delay()` 提交 + 轮询真实进度 + 断开只停轮询
- **话术**: "长任务该进队列,WS 只当看板。单线程事件循环的隔离 = 永不阻塞"

### 4. worker 挂了永久排队(Q7 后续)
- **修法**: `worker_available()` 探活(control.ping)+ 120s 卡死兜底 + async 接口 503
- **话术**: "提交前探活,没 worker 直接报错,别让任务永远排队"

### 5. 假进度 → 真实节点进度(Q7 后续)
- **修法**: `build_graph(progress_callback)` 给每个节点包一层,worker 上报 15→30→…→95
- **话术**: "进度数字来自执行方(worker)上报,不是前端/轮询写死的"

### 6. MCP 任意文件读取 + prompt injection(Q8)
- **问题**: `review_file("/app/.env")` 能把 API key 喂进 LLM 并出现在报告里
- **修法**: 三层——敏感文件黑名单(读都不读)+ 内容脱敏(sk-xxx→sk-***)+ 路径白名单(只审项目内)
- **话术**: "gitignore 挡不住服务器读自己;给了 AI 工具就是给了钥匙,必须限范围;prompt injection 不能靠'让 LLM 小心',要在数据进 LLM 前拦"

### 7. IP 限流三个洞(Q9)
- **问题**: 内存只进不出 / 反代后全 127.0.0.1 / 多进程各记各的
- **修法**: 限流移 nginx(`limit_req`,真实 IP + 共享内存 + 有界),删掉 app 旧中间件;关 8000 后门(绑 127.0.0.1)
- **话术**: "当一个更好的方案接管职责,旧代码最好的修复是删掉它"

---

## 四、系统里的 7 个限制清单

| # | 限制 | 值 | 限什么 |
|---|---|---|---|
| 1 | nginx `limit_req zone=api` | 10 次/分/IP(burst 5) | `/api` HTTP 请求(防刷接口烧钱) |
| 2 | nginx `limit_req zone=general` | 60 次/分/IP(burst 20) | 页面/静态/WS 请求 |
| 3 | Celery `--concurrency` | 4 个进程 | 同时跑几个审查任务 |
| 4 | `LLM_MAX_CONCURRENCY` | 16(每进程) | 单进程同时几个 LLM 调用在飞 |
| 5 | LLM 单次重试 | 2 次(间隔 2s) | 一个 LLM 调用失败重试 |
| 6 | Celery 任务重试 | 2 次(退避 5/10s) | 一个审查任务失败重跑 |
| 7 | 熔断器 | 连败 3 次 → 熔断 30s | LLM 挂了别硬打,防雪崩 |

**未做(面试可提思路)**:全局审查上限(Redis 计数)+ 鉴权配额——防"换 IP + 造号"。

**并发数学**:4 进程 × 每审查 4 个 LLM = 峰值 16 并发;DeepSeek 上限 500–2500,余量十几倍。信号量 16 每进程,worker 每进程只用 4 → 永不排队。

---

## 五、必背参考答题

### Q9 三个洞 + 解法
> ① 内存:dict 只进不出,换 IP 刷 → 无限膨胀。修复:LRU 或 Redis 过期
> ② 反代:client.host 是直接连接 IP,经 nginx 全变 127.0.0.1,所有人挤一个桶。要真实 IP 读 X-Forwarded-For,但只信自家 nginx
> ③ 多进程:dict 在进程内存,4 进程 4 份,分散打就绕过。状态放共享存储(Redis/nginx 共享内存)

### 换 IP 刷怎么办
> IP 限流挡不住代理池。真正要 **鉴权 + 每账号配额**,再配 **全局上限** 兜底(防造号),IP 限流只是地基。

### 为什么删掉 app 限流
> nginx 接管后,旧中间件又冗余又有害(全 127.0.0.1 共享桶会误伤),删掉 = 限流职责唯一归 nginx。

---

## 六、面试万能话术

1. **"失败要响亮"** —— 错误别静默吞掉,要暴露给用户判断
2. **"发现 bug → 讲清原理 → 动手修复 → 反思边界"** —— 比背概念强十倍
3. **"并发数学张口就来"** —— 4 进程 × 4 LLM = 16,DeepSeek 500+,余量足
4. **"限流四层"** —— 网关/任务/LLM/重试熔断,各管一段
5. **"给 AI 工具 = 给钥匙,必须限范围"** —— MCP 安全的核心

---

## 七、线上现状(截至 2026-08-02)

- ✅ nginx limit_req 限流已部署验证(/api 10r/m,连打 8 次第 7/8 次 503)
- ✅ 8000 后门已关(绑定 127.0.0.1,外网直连失败,80 走 nginx 正常)
- ✅ 敏感文件黑名单 + 脱敏 + 白名单已提交
- ⬜ 删 app 旧限流中间件(**未部署**,下次 deploy.bat 带上)
- ⬜ redis/db 端口收内网(可选,安全组挡着)

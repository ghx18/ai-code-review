apiVersion: v1
kind: ConfigMap
metadata:
  name: aicode-config
# 非敏感的配置放 ConfigMap；敏感信息（API key、密码）放 Secret
data:
  PYTHONPATH: "/app"
  DATABASE_URL: "postgresql+psycopg2://postgres:postgres@db:5432/ai_code_review"
  REDIS_URL: "redis://redis:6379/0"
  LLM_MAX_CONCURRENCY: "4"

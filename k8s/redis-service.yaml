apiVersion: v1
kind: Service
metadata:
  name: redis          # api 的 REDIS_URL 里写的就是 redis:6379
  labels:
    app: aicode
    component: redis
spec:
  type: ClusterIP
  selector:
    app: aicode
    component: redis
  ports:
    - port: 6379
      targetPort: 6379
